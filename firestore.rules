rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 클라이언트는 Firestore를 직접 건드리지 않는다.
    // 업로드/조회는 전부 Cloud Function(Admin SDK, 이 규칙을 우회함)을 통해서만 이뤄진다.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
